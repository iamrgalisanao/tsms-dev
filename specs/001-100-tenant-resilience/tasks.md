# Tasks: 100 Tenant Ingestion Resilience

**Input**: Design documents from `specs/001-100-tenant-resilience/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/

**Tests**: Required for each release-blocker remediation before implementation is considered complete.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing.

## Phase 1: Setup and Baseline

**Purpose**: Establish visibility and protect current fallback state before changing ingestion flow.

- [ ] T001 Document current official and batch ingestion flow in `specs/001-100-tenant-resilience/research.md`
- [ ] T002 [P] Add baseline log/metric inventory for official and batch ingestion in `specs/001-100-tenant-resilience/contracts/operational-signals.md`
- [ ] T003 [P] Confirm rollback/fallback branch reference in deployment notes or runbook under `docs/`
- [x] T003a [P] Add fallback-branch integrity guard script that records the expected `origin/remove-webapp-forwarding` commit SHA in `specs/001-100-tenant-resilience/rollback-baseline.txt`, fails if the current remote fallback tip differs from that SHA unless the baseline is intentionally updated, and verifies the recorded baseline is an ancestor of the release branch, in `scripts/verify-rollback-branch.sh`
- [x] T003b [P] Register the fallback-branch integrity guard as a blocking pull-request job in `.github/workflows/ci.yml` with `fetch-depth: 0` and document usage plus baseline-update procedure in `specs/001-100-tenant-resilience/quickstart.md`
- [x] T004 [P] Add focused test plan references for current backpressure and routing foundation in `specs/001-100-tenant-resilience/quickstart.md`

---

## Phase 2: Foundational Remediation

**Purpose**: Shared primitives required by all resilience stories.

- [ ] T005 Add config entries for ingestion payload limit, batch limit, enforce/degraded mode, fairness limits, and circuit breaker Redis backend in `config/tsms.php`
- [ ] T006 [P] Add/adjust queue router architecture test to prevent hardcoded `% 8` dispatch paths in `tests/Unit/`
- [ ] T007 [P] Add shared ingestion response/rejection contract tests in `tests/Feature/`
- [x] T008 Add migration for durable ingestion request state in `database/migrations/`
- [x] T009 Adapt the existing `TransactionIntake` model in `app/Models/TransactionIntake.php` and its `transaction_intake` schema migrations; do not create a parallel ingestion request model/table unless a later architecture decision explicitly replaces `transaction_intake`
- [x] T009a [P] Correct `specs/001-100-tenant-resilience/data-model.md` so the current persisted Ingestion Request entity matches the real `transaction_intake` schema (`payload_checksum`, `trace_id`, split `intake_status`/`processing_status`, `attempt_count`, `duplicate_of_intake_id`, `source_ip`); move router-computed `queue`/`shard` to `contracts/operational-signals.md`, keep `source`/`accepted_at` only as future-schema candidates, and flag the `X-Request-Id` versus `X-Correlation-ID` header divergence as an open design question
- [ ] T010 Add shared DTO/result object for backpressure decisions in `app/Services/`
- [ ] T011 Add correlation ID propagation for ingestion requests in `app/Http/Middleware/` or existing request handling path

**Checkpoint**: Foundation ready; user stories can proceed.

---

## Phase 3: User Story 1 - Protect Official Intake During Overload (Priority: P1) MVP

**Goal**: Official ingestion rejects or accepts durably before expensive DB validation/write work.

**Independent Test**: Saturate queue/backpressure and prove `/api/v1/transactions/official` rejects before DB-backed terminal existence validation or transaction persistence.

### Tests

- [x] T012 [P] [US1] Add feature test proving official overload gate runs before FormRequest `exists` validation in `tests/Feature/IngestionBackpressureTest.php`
- [x] T013 [P] [US1] Add feature test for async official intake `202 Accepted` and dispatch-after-commit in `tests/Feature/OfficialAsyncIntakeTest.php`
- [x] T014 [P] [US1] Add idempotency/conflict tests for `submission_uuid` and payload hash in `tests/Feature/OfficialAsyncIntakeIdempotencyTest.php`

### Implementation

- [x] T015 [US1] Add lightweight official intake request or middleware that performs cheap structural checks before DB validation in `app/Http/Requests/` or `app/Http/Middleware/`
- [x] T016 [US1] Implement official async intake boundary service in `app/Services/`
- [x] T017 [US1] Update official route/controller flow in `routes/api.php` and `app/Http/Controllers/API/V1/TransactionController.php`
- [x] T018 [US1] Persist durable intake state and dispatch intake job after commit in `app/Services/` and `app/Jobs/`
- [x] T019 [US1] Preserve compatibility response/status lookup behavior in `app/Http/Controllers/API/V1/`
- [x] T019a [P] [US1] Test unified 409 `IDEMPOTENCY_CONFLICT` shape across different-terminal submission UUID conflict, same-terminal payload-drift conflict, and intake-layer duplicate cases in `tests/Feature/OfficialAsyncIntakeIdempotencyTest.php`
- [x] T019b [US1] Reconcile submission UUID conflict responses onto the contract's `IDEMPOTENCY_CONFLICT` shape while preserving the existing separate `DUPLICATE_RECEIPT_CONFLICT` and `SUBMISSION_ALREADY_REJECTED` semantics in `app/Http/Controllers/API/V1/TransactionController.php`, `app/Services/TransactionIntakeService.php`, and `specs/001-100-tenant-resilience/contracts/ingestion-api.md`
- [x] T018a [US1] Fix `ProcessTransactionIntakeJob` batch-item orchestration so later item failures cannot strand earlier committed-but-undispatched transactions; dispatch each successful item's `ProcessTransactionJob` immediately after its successful `ingest()` result, continue through item failures, reserve `FAILED_PERMANENT` for zero-success batches, and cover with `tests/Feature/ProcessTransactionIntakeJobBatchFailureTest.php`
- [x] T019c [US1] Handle concurrent duplicate `transaction_intake.submission_uuid` insert races in `TransactionIntakeService::handleOfficialIntake()` by catching unique-constraint failures and resolving through the existing 202/409 idempotency response path in `tests/Feature/OfficialAsyncIntakeIdempotencyTest.php`
- [x] T019d [US1] Restore adjustment/tax child shape and required type-presence validation in `TransactionIntakeService::officialStructuralRules()` and its validation after-hook, covering malformed details without accepting a queued intake
- [x] T019e [US1] Restore registered-vs-submitted hardware ID mismatch validation in `TransactionIntakeService`, preserving `403 HARDWARE_ID_MISMATCH` for single and batch official intake payloads
- [x] T020 [US1] Add reconciliation handling for accepted-but-not-queued/queued-but-not-processed intake states in `app/Console/Commands/`
- [x] T020a [P] [US1] Test p95 transaction duration and prove the outer official ingestion transaction is not held open across non-atomic validation, logging, notification, or per-item work in `tests/Feature/OfficialIngestionTransactionBoundaryTest.php`
- [x] T020b [US1] Split the outer `storeOfficial` DB transaction so request-scope work, validation, logging, notifications, and per-item writes do not hold one long transaction open, extending the existing per-item savepoint pattern in `app/Http/Controllers/API/V1/TransactionController.php` or its Phase 1 replacement service
- [x] T020c [US1] Add or confirm indexes for tenant, terminal, external transaction ID, receipt/date, status, and timestamp lookups used by the shortened official ingestion path in `database/migrations/`

**Checkpoint**: Official ingestion no longer depends on the large synchronous write path for request acceptance.

---

## Phase 4: User Story 2 - Bound Payload, Batch, and Transaction Work (Priority: P1)

**Goal**: Reject oversized work before memory, validation, or DB fanout becomes dangerous.

**Independent Test**: Over-limit payloads and batches are rejected before persistence; boundary-limit requests proceed.

### Tests

- [x] T021 [P] [US2] Add payload byte limit tests in `tests/Feature/IngestionPayloadLimitTest.php`
- [x] T022 [P] [US2] Add batch transaction count limit tests in `tests/Feature/IngestionBatchLimitTest.php`
- [x] T023 [P] [US2] Add boundary tests for exactly max payload and max batch count in `tests/Feature/IngestionLimitBoundaryTest.php`

### Implementation

- [x] T024 [US2] Add early payload-size middleware in `app/Http/Middleware/`
- [x] T025 [US2] Register payload-size middleware on ingestion routes in `routes/api.php` or route middleware configuration
- [x] T026 [US2] Add batch count validation before per-item loops in `app/Http/Controllers/API/V1/TransactionController.php` or async intake validator
- [x] T027 [US2] Add stable error codes for `PAYLOAD_TOO_LARGE` and `BATCH_LIMIT_EXCEEDED` in ingestion response helpers

**Checkpoint**: A single oversized request cannot monopolize request/DB resources.

---

## Phase 5: User Story 3 - Shared Failure Controls and Safe Backpressure (Priority: P1)

**Goal**: Backpressure and circuit breaker decisions are shared and fail safely.

**Independent Test**: Simulate Redis/dependency failure and confirm shared breaker state plus bounded rejection in enforce mode.

### Tests

- [x] T028 [P] [US3] (F-NEW-1/F-NEW-2 resolved: a follow-up code review found the open→half-open transition itself was still an unguarded, non-atomic sequence — vulnerable to the same class of race F1/F2 eliminated elsewhere — and that the two "interleaving" tests didn't actually simulate a race window mid-operation. F-NEW-1 fixed by wrapping the transition in a new atomic `CircuitBreaker::HALF_OPEN_TRANSITION_SCRIPT`: a single Lua script does a compare-and-set guard read of `state`/`opened_at` and, only if still `open` and expired, performs the entire initialization (bump `half_open_generation`, reset counters/`failure_count`, admit the initiator as probe 1) atomically; any other caller whose script invocation runs after the winner's observes the already-half-open state, performs zero mutation, and `isAvailable()` falls through to the existing `HALF_OPEN_ADMISSION_SCRIPT` path to admit it as a normal probe against the now-current generation. F-NEW-2 fixed by renaming/rewriting `test_stale_success_after_generation_rollover_is_a_noop` → `test_success_recorded_against_a_generation_that_already_rolled_over_is_discarded` and `test_admission_reflects_the_generation_actually_current_when_a_rollover_has_already_happened` → `test_admission_after_generation_already_rolled_over_joins_the_current_generation` (docblocks corrected to state they verify a pre-set-state outcome, not interleaving/atomicity), and adding `test_losing_caller_in_open_to_half_open_transition_joins_winning_generation_instead_of_clobbering_it`, which directly invokes the protected `transitionToHalfOpen()` twice to prove exactly one generation increment occurs and the losing caller correctly joins rather than clobbers — with an explicit docblock caveat that this does not and cannot prove real Redis EVAL concurrency, only the script's own CAS-guard correctness.) (F1/F2 resolved: `recordSuccess()`/`recordFailure()`'s half-open branch and `isAvailable()`'s half-open admission branch were reworked to single atomic Redis Lua script executions — `CircuitBreaker::HALF_OPEN_OUTCOME_SCRIPT` and `CircuitBreaker::HALF_OPEN_ADMISSION_SCRIPT` — closing the non-atomic check-then-mutate window that could contaminate a rolled-over generation's counters or misattribute a probe's admitted generation; see the new interleaving tests `test_stale_success_after_generation_rollover_is_a_noop` and `test_admission_reflects_the_generation_actually_current_when_a_rollover_has_already_happened` in `tests/Unit/RedisCircuitBreakerTest.php`) Add unit tests for the real Redis-backed `App\Services\CircuitBreaker` (closed/open/half-open transitions via `isAvailable()`, `recordSuccess()`, `recordFailure()`, `transitionToHalfOpen()`) in a NEW file `tests/Unit/RedisCircuitBreakerTest.php`. Do NOT reuse or extend `tests/Unit/CircuitBreakerTest.php` — that file tests the unrelated legacy DB-backed `App\Models\CircuitBreaker` model and must stay as-is, covering only that model. T028a is resolved (bounded concurrent probes, N=3, 2-of-3 close/reopen, `half_open_generation`-based staleness) — treat `specs/001-100-tenant-resilience/adr/T028a-half-open-circuit-breaker-semantics.md` as the source of truth for exact semantics rather than re-deriving them here. Required generation-safety coverage includes: (1) late failure recorded after the circuit already closed must not reopen it; (2) late success recorded after the circuit already reopened must not close it; (3) probe admission is capped at N=3 per generation, with the 4th+ concurrent admission attempt rejected; (4) circuit closes on 2-of-3 successes; (5) circuit reopens on 2-of-3 failures; (6) an abandoned probe (no `recordSuccess()`/`recordFailure()` ever called) does not wedge the breaker in `half-open` past `resetTimeoutSeconds` — the next caller observes expiry and starts a fresh generation; (7) concurrent requests simultaneously reaching the close/reopen threshold produce a single idempotent, non-corrupting transition (no double-close, no double-reopen, no lost counter updates).
- [x] T028a [P] [US3] Decide half-open circuit-breaker semantics for `App\Services\CircuitBreaker`. **Resolved** — decision recorded and approved in `specs/001-100-tenant-resilience/adr/T028a-half-open-circuit-breaker-semantics.md` (bounded concurrent probes per circuit key, N=3, close on 2-of-3 successes, reopen on 2-of-3 failures, `half_open_generation` for late-result/staleness handling, `failure_count` reset on entry to half-open). The ADR is the approved decision, not an open question. `specs/001-100-tenant-resilience/data-model.md`'s Circuit Breaker State fields have been reconciled against it.
- [x] T028b [US3] Add a `Retry-After` header (and matching JSON body field, consistent with the pattern established by T035's fix) to `CircuitBreakerMiddleware::handle()`'s rejection response (`app/Http/Middleware/CircuitBreakerMiddleware.php`, currently a bare `response()->json([...], 503)` with no `Retry-After` header) for both cases: the circuit is `OPEN`, and a `HALF_OPEN` request is rejected for being over the N=3 probe cap established by T028a. This is a separate, pre-existing gap unrelated to half-open probe correctness — out of scope for T028/T028a per the ADR — and must not be conflated with T035, which fixes the unrelated intake-aggregate-overload `Retry-After` gap in `TransactionController::storeOfficial()`. Require a regression test asserting the header and JSON body carry the same value for both the OPEN and over-cap HALF_OPEN rejection cases.
  - Done: `CircuitBreaker::retryAfterSeconds()`/`computeRetryAfterSeconds()` added (derives seconds-remaining from `opened_at`/`half_open_started_at` + `resetTimeoutSeconds`, floored at 1, mirroring `IngestionBackpressureService`'s clamp convention); `CircuitBreakerMiddleware` now attaches an identical `retry_after_seconds` JSON field and `Retry-After` header computed once. New tests in `tests/Feature/IngestionCircuitBreakerTest.php` cover OPEN and over-cap HALF_OPEN rejections (header/body identity) and a degenerate-`opened_at` clamp case. All listed regression suites pass.
- [x] T029 [P] [US3] Add feature test for Redis/backpressure unavailable in enforce mode in `tests/Feature/IngestionBackpressureFailureModeTest.php` — satisfied by `test_route_level_processing_check_returns_503_ingestion_degraded_when_redis_throws_in_enforce_mode` (line 32), `test_official_endpoint_returns_503_ingestion_degraded_when_intake_redis_check_throws_in_enforce_mode` (line 73), and `test_official_endpoint_returns_503_ingestion_degraded_when_both_queues_are_unevaluable` (line 111) — each asserts a `503 INGESTION_DEGRADED` response when the Redis health check throws in enforce mode.
- [x] T030 [P] [US3] Add tests proving validation `4xx` failures do not open breaker state in `tests/Feature/IngestionCircuitBreakerTest.php` — satisfied by `test_validation_failures_do_not_open_the_breaker` (line 45) and `test_backpressure_rejections_do_not_open_the_breaker` (line 68), which assert repeated validation/backpressure 4xx rejections leave the breaker closed, consistent with FR-014's infrastructure-vs-client-failure distinction.
- [x] T031 [P] [US3] Add response consistency test for clamped retry header/body — satisfied by `test_official_endpoint_returns_429_with_retry_metadata_when_backpressure_is_enforced` (`tests/Feature/IngestionBackpressureTest.php:63`), asserting the official/processing-overload path's `Retry-After` header and JSON `retry_after_seconds` match, and `test_degraded_response_body_and_retry_after_header_use_the_same_clamped_value` (`tests/Feature/IngestionBackpressureFailureModeTest.php:150`), asserting the same for the degraded/clamped-to-1 case on the batch route. Note: this task's originally-listed single file undersold actual coverage — both files are cited as real evidence. This does NOT cover the intake-aggregate-overloaded/processing-healthy path through `storeOfficial()`; T035 addresses that gap separately.

### Implementation

- [x] T032 [US3] Replace or extend local circuit breaker service with Redis-backed state in `app/Services/CircuitBreaker.php`
- [x] T033 [US3] Record breaker success/failure around ingestion dependencies in `app/Http/Middleware/CircuitBreakerMiddleware.php` and ingestion services/jobs; no ingestion code path currently records outcomes against this breaker
- [x] T034 [US3] Update `IngestionBackpressureService` to return fail-closed/degraded decisions in enforce mode in `app/Services/IngestionBackpressureService.php`
- [x] T034a [US3] Define and implement the official ingestion backpressure aggregation policy for intake and processing queues in `rejectWhenProcessingBackpressureEnforced()` or its Phase 1 replacement: after T034, when backpressure is enabled and in enforce mode, reject if either queue is overloaded, return degraded if either required health check cannot be evaluated, exclude disabled-mode decisions from degraded aggregation, and include both `backpressure.intake` and `backpressure.processing` sub-decisions in response/log context
- [x] T035 [US3] Fix the missing `Retry-After` header on the official intake-overload response path. `TransactionController::storeOfficial()` (`app/Http/Controllers/API/V1/TransactionController.php:840-846`) returns `TransactionIntakeService::handleOfficialIntake()`'s rejection/degraded payload via a bare `response()->json($result, $httpStatus)` with no `Retry-After` header, unlike `IngestionBackpressureMiddleware::handle()` (`app/Http/Middleware/IngestionBackpressureMiddleware.php:38-47`), which does attach it. So when the *intake* queue (not processing) is overloaded and `TransactionIntakeService::handleOfficialIntake()`'s own `checkAggregate()` call (`app/Services/TransactionIntakeService.php:179-197`) produces the rejection/degraded result, clients get `retry_after_seconds` in the JSON body but no `Retry-After` header — violating FR-009/contracts/ingestion-api.md. Add the header where `storeOfficial()` builds its response (or centralize header-attachment in a shared response helper used by both the middleware and the service-returned payload). Require a regression test covering the intake-overloaded/processing-healthy case through the full HTTP path (`POST /api/v1/transactions/official` with only intake queue depth exceeded, processing healthy) asserting both the JSON body and the `Retry-After` header carry the same clamped value, in `tests/Feature/IngestionBackpressureTest.php` or a new focused test file. — Implemented: `storeOfficial()` now attaches `->header('Retry-After', (string) $result['retry_after_seconds'])` conditionally, only when that key is present (rejection/degraded responses only, never on normal 202 acceptance). Extended `test_official_endpoint_aggregate_rejection_reports_both_intake_and_processing_subdecisions_when_only_intake_is_overloaded` (`tests/Feature/IngestionBackpressureTest.php:168`) with `assertHeader('Retry-After')` plus a real equality assertion between `$response->headers->get('Retry-After')` and `(string) $response->json('retry_after_seconds')`. `tests/Feature/IngestionBackpressureTest.php`: 8 passed (68 assertions).
- [x] T036 [US3] Add breaker/backpressure runbook in `docs/` that explicitly documents every identified circuit-breaker operational surface and states which ones do NOT reflect real ingestion breaker state: (1) the real ingestion circuit breaker — `App\Services\CircuitBreaker`, Redis-backed, key `tsms:circuit-breaker:transaction-intake`, wired into ingestion via `app/Http/Middleware/CircuitBreakerMiddleware.php:22` and `routes/api.php:184-186` — the only source of truth for ingestion breaker state, inspectable via `redis-cli HGETALL tsms:circuit-breaker:transaction-intake`; (2) `app/Http/Controllers/API/V1/CircuitBreakerController.php` — queries the legacy `App\Models\CircuitBreaker` DB-backed model, does NOT reflect real ingestion breaker state; (3) `app/Http/Controllers/API/Dashboard/CircuitBreakerController.php` — also queries the legacy `App\Models\CircuitBreaker`, does NOT reflect real ingestion breaker state; (4) the filesystem dashboard already flagged in `contracts/operational-signals.md` (`storage_path('framework/circuit-breakers')`), which `CircuitBreaker.php` no longer writes to and is stale/non-functional. The runbook must instruct operators to use Redis inspection or the real metric/alert — never surfaces (2)-(4) — when diagnosing ingestion breaker incidents. **Status**: Implemented as `docs/CIRCUIT_BREAKER_BACKPRESSURE_RUNBOOK.md` (gitignored by the repo's blanket `*.md` rule, same as the T028a ADR and T047 shard runbook — exists on disk, not yet tracked). Covers all 11 required sections (purpose/scope distinguishing breaker vs. backpressure, authoritative surfaces, all three legacy/misleading surfaces with an explicit "never use" rule, full CLOSED/OPEN/HALF_OPEN state description including the N=3/2-of-3/generation/stale-outcome/expiry semantics and the three-Lua-script atomicity, backpressure enforce-vs-observe/fail-closed-in-enforce/Retry-After behavior including the T035 fix, exact Redis key/field table, log-message-based diagnosis with real safe read-only commands, recovery procedures per failure mode, an unsafe-actions list, a validation section citing real test files, and a change-history section). Every class name, Redis key, config path, queue-naming convention, Horizon supervisor name, and Artisan/`horizon:*`/`redis-cli` command was verified against the actual current source (`CircuitBreaker.php`, `CircuitBreakerMiddleware.php`, `IngestionBackpressureService.php`, `IngestionBackpressureMiddleware.php`, `TransactionIntakeService.php`, `TransactionController::storeOfficial()`, `IngestionQueueRouter.php`, `config/tsms.php`, `config/horizon.php`, and the real Horizon console command list) before being written, not assumed. Two facts genuinely not derivable from this repository (production alerting/dashboard wiring, and real-world dependency recovery time) are explicitly marked "Unknown / Not Verifiable From This Repository" rather than guessed.

**Checkpoint**: Overload control protects the system consistently across app instances.

---

## Phase 6: User Story 4 - Fair Multi-Tenant Queue Processing (Priority: P2)

**Goal**: Ensure deterministic routing, isolated workers, and tenant/terminal fairness.

**Independent Test**: One hot tenant cannot starve 99 normal tenants in staging.

### Tests

- [x] T037 [P] [US4] Add architecture/static test in `tests/Unit/` that explicitly forbids hardcoded modulo sharding patterns like `% 8` (or any literal `% <shard_count>` dispatch) anywhere in `app/` — not just generically "no hardcoded dispatch." The test must scan `app/` source for the literal pattern and fail if found outside `IngestionQueueRouter`'s own implementation, explicitly covering the known offenders (`app/Http/Controllers/TestTransactionController.php`, `app/Http/Controllers/API/V1/RetryHistoryController.php`, `app/Jobs/BulkGenerateTransactionsJob.php`, `app/Console/Commands/TestTransactionPipeline.php`) as well as guarding against future reintroduction. Implemented via `PhpToken`-based tokenizer scan in `tests/Unit/TenantShardModuloStaticAnalysisTest.php`; passes clean against current `app/` (confirming T040-T043) and includes a self-test fixture proving the detector actually flags the dangerous shape.
- [x] T038 [P] [US4] Add feature tests for tenant and terminal fairness rejections in `tests/Feature/IngestionFairnessTest.php`, written against T044's fairness service interface (no I/O dependency on T044's Redis keys directly — assert observable behavior: rejection status/body/reason, `Retry-After` presence, fail-open on simulated Redis failure). Required cases: (1) a tenant exceeding its configured fixed-window limit is rejected while a different tenant is unaffected; (2) a terminal exceeding its configured fixed-window limit is rejected while a sibling terminal on the *same* tenant is unaffected (proves per-terminal, not just per-tenant, scoping); (3) fairness Redis `INCR`/`EXPIRE` throwing an exception fails OPEN (request proceeds) rather than rejecting; (4) a fairness rejection's JSON body `retry_after_seconds` and `Retry-After` header carry the same value, derived from the active window's reset time (mirrors T035/T028b). Tests may be authored in parallel with T044 (both are otherwise unblocked) but assert against T044's actual public interface once it exists. — Implemented as `tests/Feature/IngestionFairnessTest.php` (8 scenarios: hot-tenant isolation, sibling-terminal isolation, global-scope independence, exact-limit boundary crossing, retry_after_seconds/reset_at correctness, fail-open across a call sequence, store-level key isolation, and a static guard against coupling to `IngestionQueueRouter`), exercising `App\Services\IngestionFairnessService` directly against real `config('tsms.fairness')` limits (no middleware/route exists yet — that is T045). Reused T044's unit-test Redis/Lua fake by extracting it into `tests/Traits/FakesIngestionFairnessRedis.php`, now shared by both `tests/Unit/IngestionFairnessServiceTest.php` and this feature test. Post-review fix (findings F1/F2): the exact-limit/global-scope boundary scenarios originally reached their preconditions by looping real simulated `eval()` calls up to the actual configured limit (up to 10,000 for the global scope), and the fake's `eval()` closure asserted script identity on every single one of those calls — together inflating the feature suite to ~10,458 assertions over ~9.33s. Fixed by adding `FakesIngestionFairnessRedis::seedFairnessCount()`, a direct state-seeding helper (mirroring `FakesCircuitBreakerRedis`'s pattern) that writes straight into the fake's counter store under the exact `{key_prefix}{scope}:{scope_id}:{window}` key the service will compute, so each boundary scenario now seeds `limit - 1` and makes exactly 2 real service calls (allowed at the limit, rejected one past it) instead of looping. The per-call script-identity assertion was removed from the `eval()` closure's hot path and replaced with a one-time-per-test `assertFairnessScriptWasUsed()` check. Same test names and asserted outcomes preserved throughout — only the seeding mechanism changed. Result: `tests/Feature/IngestionFairnessTest.php` now 8 passed/98 assertions in ~7.5s (dominated by one-time `RefreshDatabase` migration bootstrap, not fairness logic). `tests/Unit/IngestionFairnessServiceTest.php` remains 12 passed but dropped to 65 assertions (from 96) as a direct, expected consequence of removing the shared fake's per-call assertion — no unit test file was edited; the count change is solely the shared trait fix taking effect there too.
- [x] T039 [P] [US4] Add a Horizon config characterization test in `tests/Feature/` (e.g. `tests/Feature/HorizonStagingSupervisorSeparationTest.php`) that reads `config('horizon.environments.staging')` as a plain PHP array (no live Horizon/Redis process involved — deterministic, non-flaky) and asserts: (1) build a map from each of four concern queue-name-sets — **processing** (queue names matching `transaction-processing` or `transaction-processing:s<N>`), **low** (`['low']`), **notifications** (`['notifications']`), **reporting** (`['reporting']`) — to the single supervisor key within staging whose `queue` array contains every queue name in that concern's set; (2) assert each of the four concerns resolves to exactly one supervisor key (fail if a concern's queues are missing from staging, or split across more than one supervisor); (3) assert the four resolved supervisor keys are pairwise distinct, i.e. `count(array_unique([$processingSupervisorKey, $lowSupervisorKey, $notificationsSupervisorKey, $reportingSupervisorKey])) === 4` — this is what actually proves processing/low/notifications/reporting no longer share one worker pool (today they do not hold, since staging's current `default` supervisor hosts processing+low+notifications together — this test is expected to fail until T046 lands, which is the point of a characterization test gating T046's completion). **T046 must not be marked `[x]` until this test exists and passes** — see T046 below and the Dependencies section. **Status**: Implemented in `tests/Feature/HorizonSupervisorSeparationTest.php`, reading `config('horizon.environments.staging')` structurally (no raw-text matching). Correctly characterized the target topology and was red pending T046 (`test_the_four_concern_supervisor_keys_are_pairwise_distinct` and `test_staging_default_supervisor_does_not_span_more_than_one_concern` failed because staging's `default` supervisor hosted processing+low+notifications together). **Now passing 6/6 (12 assertions)** against T046's real staging supervisor split in `config/horizon.php` — no test-expectation changes were needed; the test file is unmodified.

### Implementation

- [x] T040 [US4] Replace `% 8` routing in `app/Http/Controllers/TestTransactionController.php` — `process()` and `retryTransaction()` now dispatch via `IngestionQueueRouter::processingQueueForTenant()`; covered by `tests/Feature/TenantShardRoutingConsistencyTest.php`
- [x] T041 [US4] Replace `% 8` routing in `app/Http/Controllers/API/V1/RetryHistoryController.php` — `retrigger()` and `retry()` now dispatch via `IngestionQueueRouter::processingQueueForTenant()`; covered by `tests/Feature/TenantShardRoutingConsistencyTest.php`
- [x] T042 [US4] Replace `% 8` routing in `app/Jobs/BulkGenerateTransactionsJob.php` — `handle()` now dispatches via `IngestionQueueRouter::processingQueueForTenant()`; covered by `tests/Feature/TenantShardRoutingConsistencyTest.php`
- [x] T043 [US4] Replace `% 8` routing in `app/Console/Commands/TestTransactionPipeline.php` — `handle()` now dispatches via `IngestionQueueRouter::processingQueueForTenant()`; covered by `tests/Feature/TenantShardRoutingConsistencyTest.php`
- [x] T044 [US4] Implement Redis global/tenant/terminal fairness service in `app/Services/` (e.g. `FairnessService`) using fixed-window `INCR`+`EXPIRE` counters per scope (global, tenant, terminal) — not a token-bucket, not a new Lua script; a rate window is a single atomic increment, not a multi-step state race, consistent with the codebase's existing complexity ceiling (Lua reserved for genuine races like the circuit breaker's half-open admission). Scope per-tenant AND per-terminal, never per-shard: at the configured shard count (`config('tsms.processing.shard_count')`, currently 8) and ~100 tenants, roughly 12-13 tenants collide per shard, so a shard-level limit alone would still let one hot tenant starve its shard-mates. MUST fail OPEN when its Redis operations fail (diverges from `IngestionBackpressureService`'s fail-closed-in-enforce-mode convention — see plan.md's "Fairness Architecture" subsection for the full rationale). Rejections MUST carry a `Retry-After` derived from the active window's reset time, mirroring T035/T028b. Keep this service a separate abstraction from `IngestionQueueRouter`: the router stays a pure, I/O-free function answering "which queue"; this service owns I/O and answers "allow or throttle now" — do not merge them. Tenant-tier overrides are explicitly OUT OF SCOPE/deferred for this task — see plan.md's "Fairness Architecture" subsection. No blocking dependency; can start immediately. — Implemented as `App\Services\IngestionFairnessService` (`checkGlobal()`/`checkTenant()`/`checkTerminal()`), config in `config/tsms.php`'s new `fairness` block. Post-review hardening (F1/F2/F3): the per-scope INCR+conditional-EXPIRE is now executed as a single atomic Redis Lua script (`FAIRNESS_CHECK_SCRIPT`, following `CircuitBreaker`'s existing `eval()` convention) instead of two separate round-trips in one try/catch, eliminating a partial-failure window where a successful INCR followed by a throwing EXPIRE discarded the real count and reported `count=0`; covered by `tests/Unit/IngestionFairnessServiceTest.php` (12 tests, 96 assertions, all passing).
- [x] T045 [US4] Apply fairness before enqueue in official and batch ingestion paths: add a fairness middleware and register it on `/api/v1/transactions/official` and `/api/v1/transactions/batch` in `routes/api.php`, positioned AFTER `ingestion.payload_size`, `ingestion.backpressure:processing`, and `circuit.breaker:transaction-intake` in the middleware list (fairness runs last — cheapest/most-global checks run first, the most granular/expensive per-tenant+terminal check runs last, only for requests that already passed system-wide health gates). Resolve tenant/terminal identity exactly per plan.md's "Fairness Architecture" tenant-resolution decision: primary source is the authenticated `PosTerminal`'s `tenant_id`/`id` via `$request->user()` (guaranteed present here since `auth:sanctum` + `abilities:transaction:create` already ran earlier in the same route group, per `routes/api.php:171,180`); fallback is `$request->input('tenant_id')`/`$request->input('terminal_id')`, mirroring `IngestionBackpressureMiddleware`'s existing tenant fallback convention. If tenant identity is still unresolved (not a positive integer) after fallback, SKIP the fairness check and let later validation reject the request — do not invent a fairness-specific rejection for an identity problem. Depends on T044 (needs the fairness service); should follow T038 (tests are written against T044's interface first). — Implemented as `app/Http/Middleware/IngestionFairnessMiddleware.php` (aliased `ingestion.fairness` in `bootstrap/app.php`, appended last on both routes in `routes/api.php`), covered by `tests/Feature/IngestionFairnessMiddlewareTest.php` (11 passed). Wiring fairness onto these routes made `IngestionFairnessService` issue additional `Redis::connection('default')->eval(...)` calls on every request through them, which broke three pre-existing suites whose Redis mocks asserted an exact pre-fairness `connection('default')` call total: `tests/Feature/IngestionBackpressureTest.php`, `tests/Feature/OfficialAsyncIntakeTest.php`, `tests/Feature/OfficialAsyncIntakeIdempotencyTest.php`. Fixed by relaxing each file's shared `mockRedisDepths()`/`mockQueueDepths()` helper: dropped the brittle `Redis::shouldReceive('connection')->times(count($depths))` total and instead scoped exactness to the `llen` calls those files actually care about (`->shouldReceive('llen')->once()->with(...)` per queue, unchanged), while adding `->shouldReceive('eval')->zeroOrMoreTimes()->andReturn(1)` on the same double so fairness's incidental eval calls pass through un-counted instead of colliding with the total. Fairness still genuinely executes in these tests (not disabled/bypassed); a fresh/unseeded window's simulated count of 1 is comfortably under every real configured limit (global 10000/tenant 200/terminal 50), so it naturally evaluates as allowed. All three suites now fully pass, with `tests/Feature/IngestionCircuitBreakerTest.php`, `tests/Feature/IngestionFairnessTest.php`, `tests/Unit/IngestionFairnessServiceTest.php`, and `tests/Feature/IngestionBackpressureFailureModeTest.php` confirmed unaffected.
- [x] T046 [US4] Split staging Horizon supervisors for processing, low, notifications, and reporting in `config/horizon.php` (today staging's single `default` supervisor hosts processing+low+notifications together — see `config/horizon.php`'s `staging` environment key). Gated on TWO independent conditions, both must hold before this task is marked `[x]`: (a) **T020b** — transaction-boundary splitting passing validation, before processing worker capacity is increased; (b) **T039** — the Horizon config characterization test asserting processing/low/notifications/reporting resolve to four pairwise-distinct supervisor keys must exist and pass. This task's checkbox MUST NOT be marked complete on manual inspection of `config/horizon.php` alone — T039 passing is the completion proof. **Status**: both gates satisfied — T020b is already `[x]`, and T039 now genuinely passes 6/6 against the real split. Staging's `default` supervisor (previously `[...$processingQueues, 'low', 'notifications']`, `processes => 4`) was removed and replaced with four pairwise-distinct supervisors: `processing-supervisor` (`[...$processingQueues]`, `processes => 2`), `low-supervisor` (`['low']`, `processes => 1`), `notifications-supervisor` (`['notifications']`, `processes => 1`); `reporting-supervisor` (`['reporting']`) was left untouched as it was already dedicated. `connection => 'redis'`, `balance => 'auto'`, and `tries => 2` were carried forward unchanged from the old `default` supervisor to all three new ones. Total staging `processes` across these three concerns stays at 4 (2+1+1), the same total the old combined `default` supervisor had — no worker capacity increase, respecting the T020b gate. Production's environment block in `config/horizon.php` was not touched.
- [x] T046a [P] [US4] Fix a pre-existing implicit default-queue gap surfaced by a code-review safety check performed alongside T046 (the gap predates and is independent of the supervisor split — staging's old `default` supervisor never actually consumed the literal `default` queue name either, so this is not a regression, just a real, separately-tracked bug). Several live `ShouldQueue`/`ShouldBroadcast` classes have no explicit queue assignment at all and therefore silently fall back to Redis's configured default queue (`config('queue.connections.redis.queue')`, `'default'`), which no staging or production Horizon supervisor consumes, meaning these jobs/notifications/broadcasts accumulate unprocessed indefinitely: `app/Jobs/CheckCircuitBreakerStatus.php` (scheduled every 5 minutes via `ScheduleServiceProvider`), `app/Jobs/CheckTransactionFailureThresholdsJob.php` (dispatched from `TransactionController.php:2028`), `app/Jobs/DispatchWebhookRetryJob.php` (dispatched from the `RetryFailedWebhooks` console command), `app/Notifications/BatchProcessingFailure.php`, `app/Notifications/SecurityAuditAlert.php`, `app/Notifications/TransactionResultNotification.php` (dispatched via `NotificationService`/`ProcessTransactionJob.php:295` — this one fires on every successful transaction, the highest-frequency instance of the gap), and `app/Events/TransactionUpdated.php` (a `ShouldBroadcast` event with no `broadcastQueue()` override, fired from `TransactionService.php`).

  A follow-up architecture review of this draft found that `DispatchWebhookRetryJob` and `TransactionResultNotification`'s `webhook` channel both make blocking outbound HTTP calls (`Http::timeout(10)` and `Http::timeout(30)->retry(3,1000)` respectively — up to ~93s per call for the latter) at real transaction-scale frequency, and bundling either onto the shared `low`/`notifications` queues (1 process each in staging) would starve the other, fast, low-volume work sharing those queues — exactly the starvation class this feature exists to prevent. A new dedicated `webhook-callbacks` queue/supervisor is therefore required to isolate this blocking I/O; this is an intentional, approved production topology change, not scope creep.

  **Final approved mapping** (per-class queue + assignment mechanism, matching this codebase's existing conventions):
  - `CheckCircuitBreakerStatus` → `low`, via a `public $queue = 'low';` property.
  - `CheckTransactionFailureThresholdsJob` → `low`, via a `public $queue = 'low';` property.
  - `DispatchWebhookRetryJob` → `webhook-callbacks` (new queue, see topology change below), via a `public $queue = 'webhook-callbacks';` property.
  - `BatchProcessingFailure` → `notifications`, via `viaQueues()` (mirroring the pattern already used by sibling notifications `TenantInactivityAlert`/`TransactionFailureThresholdExceeded`).
  - `SecurityAuditAlert` → `notifications`, via `viaQueues()`.
  - `TransactionResultNotification` → split by channel via `viaQueues()`: `database` channel → `notifications`; `webhook` channel → `webhook-callbacks` (new queue). Laravel dispatches one `SendQueuedNotifications` job per channel, so `viaQueues()` genuinely isolates the two.
  - `TransactionUpdated` → `notifications`, via a `broadcastQueue()` override.
  - `TransactionStatusUpdated` (dormant, no live dispatch site found) → `notifications`, via `broadcastQueue()` — pre-assigned for forward consistency so a future call site can't silently inherit the default-queue gap.
  - `TransactionRetryUpdated` (dormant, no live dispatch site found) → `notifications`, via `broadcastQueue()` — same forward-consistency rationale.

  **Required topology change** (in `config/horizon.php`, both environments — this is a deliberate, approved exception to "no topology change needed," made only because the existing `low`/`notifications` queues cannot safely absorb this specific blocking-I/O traffic): add a new `webhook-callbacks` queue owned by a new, dedicated `webhook-supervisor` in staging (1 process) and production (2 processes, for headroom against 1:1-with-transaction-volume webhook traffic). All existing supervisors and their current queue ownership (`intake-supervisor`, `processing-supervisor`, `low-supervisor`, `notifications-supervisor`, `reporting-supervisor` in staging; the equivalent set in production) must be preserved unchanged — no queue may be owned by more than one supervisor after this change.

  **Add two things**: (1) a static/architecture test (e.g. `tests/Unit/QueuedClassExplicitQueueTest.php`, following the `PhpToken`-based inspection style already established by `tests/Unit/TenantShardModuloStaticAnalysisTest.php`) that scans `ShouldQueue` classes under `app/Jobs/` and `app/Notifications/`, and `ShouldBroadcast` classes under `app/Events/`, and fails if any lacks an explicit queue assignment. An explicit assignment is any of: a non-null `public $queue` property, `$this->onQueue()` in the constructor, `viaQueues()` covering every channel the class's `via()` can emit, or a non-null `broadcastQueue()`. A `null`/unset `$queue` property is permitted ONLY when `viaQueues()` is also present and explicitly maps every channel (matching the existing pattern already used by `TransactionFailureThresholdExceeded`) — a `null`/unset `$queue` with no covering `viaQueues()` must fail the test. The required-coverage list must include all seven live classes above plus the two dormant broadcast events (`TransactionStatusUpdated`, `TransactionRetryUpdated`), so the guard holds before they're ever wired up rather than only after a future incident. (2) Horizon configuration tests (extending or sibling to `tests/Feature/HorizonSupervisorSeparationTest.php`) asserting the new `webhook-callbacks` queue resolves to exactly one dedicated supervisor in BOTH staging and production, and that no existing queue's supervisor ownership changed as a side effect of this addition.

  No blocking dependency; independent of T047 (T047 concerns shard-count-change drain safety, an unrelated risk). T046 remains complete and unchanged by this task — T046a only adds a new queue/supervisor pair alongside T046's existing four-supervisor staging split, it does not revise or reopen T046's own scope.

  **Status**: complete. The approved mapping was applied to all 9 classes (`CheckCircuitBreakerStatus`/`CheckTransactionFailureThresholdsJob`/`DispatchWebhookRetryJob` → `low`/`low`/`webhook-callbacks` via `$this->onQueue(...)` in their constructor — NOT a `public $queue` property: Queueable already declares an untyped `public $queue;` [default `null`], and PHP 8.4 treats a class re-declaring that trait property with a differing default as an incompatible composition, a hard fatal error at class-load time, not a silent override as in older PHP; `onQueue()` in the constructor is an equally explicit, approved mechanism per this task's own acceptance criteria, and is what all other queue-assigning jobs in this codebase already use; `BatchProcessingFailure`/`SecurityAuditAlert` → `notifications` via `viaQueues()`; `TransactionResultNotification` → `database`→`notifications`, `webhook`→`webhook-callbacks` via `viaQueues()`; `TransactionUpdated`/`TransactionStatusUpdated`/`TransactionRetryUpdated` → `notifications` via `broadcastQueue(): string`). `config/horizon.php` gained a new `webhook-supervisor` (`queue => ['webhook-callbacks']`) in both `staging` (`processes => 1`, matching `low-supervisor`/`notifications-supervisor`'s `connection`/`balance`/`tries` conventions) and `production` (`processes => 2`, matching `low-supervisor`'s `connection`/`balance`/`tries`/`timeout`/`nice` conventions) — purely additive, all existing supervisors and queue ownership unchanged in both environments. New static test `tests/Unit/QueuedClassExplicitQueueTest.php` (Reflection + `PhpToken`-based, no execution/instantiation of app classes) passes 10/10 (76 total assertions across it and the Horizon test below), including 8 self-test fixtures proving the detector correctly accepts/rejects each of the 4 mechanisms plus the null-`$queue`-with-`viaQueues()` exception, an explicit required-coverage assertion for all 9 classes with their exact expected mechanism, and a blanket sweep of `app/Jobs`, `app/Notifications`, `app/Events` (with one documented, out-of-scope, pre-existing exception: `ProcessTransactionIntakeJob`, whose queue is assigned entirely at its two dispatch call sites). `tests/Feature/HorizonSupervisorSeparationTest.php` was extended with 5 new tests (11/11 total, the original 6 T046/T039 assertions untouched and passing) proving `webhook-callbacks` resolves to exactly one supervisor in staging (1 process) and production (2 processes), and that the four original concerns resolve to their unchanged supervisor keys in both environments post-addition.
- [x] T047 [US4] Harden shard-count-change safety in `docs/` with an actual drain/verification/rollback procedure, not documentation-only: (1) pre-change queue-depth verification confirming all to-be-removed shard queues are drained to zero before `shard_count` is reduced; (2) a drain-before-reduce procedure — stop producers or route new work away from the shards being removed, let Horizon workers finish in-flight jobs, then verify empty; (3) a rollback path restoring the prior `shard_count` and worker supervisors if drain verification fails or orphaned jobs are found; (4) an explicit warning that reducing `shard_count` while jobs are already enqueued under `IngestionQueueRouter`'s `crc32() % shardCount` scheme (`app/Services/IngestionQueueRouter.php:18-23,40-43`) can silently and permanently orphan jobs in Redis with no live Horizon consumer, since a dispatched-but-unconsumed shard index produces no error.

  **Status**: complete. Implemented as a new read-only, report-only Artisan command `App\Console\Commands\VerifyShardTopology` (`tsms:shard-topology-verify {--to=}`) that performs zero Redis/Horizon mutations: verifies `config('tsms.processing.shard_count')` agrees with the current environment's live Horizon supervisor queue topology (discovered dynamically by scanning for `transaction-processing:s{N}` queue names, so it works regardless of which supervisor key owns processing per environment — `high-supervisor` in production, `processing-supervisor` in staging, folded into `default` locally); with `--to`, classifies the proposed change as unchanged/increase/decrease, and for decrease computes the exact retired queue set (`[new_count..old_count-1]`) and inspects each retired queue's real ready (`LLEN queues:{name}`)/reserved (`ZCARD queues:{name}:reserved`)/delayed (`ZCARD queues:{name}:delayed`) depth via Laravel's actual `RedisQueue` key convention, reporting UNSAFE with exit code 1 on any nonzero depth or any app/Horizon count mismatch, exit code 0 otherwise. Increase is reported as a non-orphaning rebalance (no queue retired) and never itself triggers the unsafe path. Paired with `docs/SHARD_COUNT_CHANGE_RUNBOOK.md`, documenting the increase procedure (report scope → deploy → restart Horizon → re-verify, with an explicit note that increases can remap tenants across existing shards even though nothing is retired) and the decrease procedure in strict order (verify retired set → hold producer config → `horizon:pause-supervisor` → poll verify until drained → cutover deploy + `horizon:terminate --wait` → re-verify), plus pre/post-cutover checklists, a two-case rollback procedure (pre-cutover clean resume via `horizon:continue-supervisor`; post-cutover env revert + Horizon restart + re-verify), and an explicit statement that this procedure does not provide exactly-once delivery — Laravel's existing reserved-job/`retry_after` redelivery behavior on worker interruption is unchanged, and duplicate-processing protection remains an application-layer idempotency concern. Tested in `tests/Unit/ShardTopologyVerifyCommandTest.php` against a new `tests/Traits/FakesShardTopologyRedis` fake (Mockery double over `Redis::connection()`, read-only `llen`/`zcard` stubs plus mutation-command stubs that throw if ever called): 12/12 passing, 184 assertions, covering matching/mismatched app-vs-Horizon counts (including a supervisor-key-agnostic case mirroring production's `high-supervisor` naming), exact retired-queue-set identification on decrease, no-false-retirement on increase, unchanged classification, independent nonzero-exit coverage for each of ready/reserved/delayed depth, safe zero-exit when all checks pass, an explicit zero-Redis-mutation assertion across every scenario, and invalid-`--to` input handling. Sanity-checked against the real dev config/Redis (`php artisan tsms:shard-topology-verify`, `--to=12`, `--to=4`) — all ran cleanly, exit code 0, zero mutations.

**Checkpoint**: Queue processing locality and tenant fairness are consistent enough for controlled 100-tenant load.

---

## Phase 7: User Story 5 - Operational Readiness for 100-Tenant Load Test (Priority: P2)

**Status: DEFERRED to a separate follow-up phase.** US1-US4 (through T047) are complete and validated. US5 (T048-T061 below) is a distinct operational-readiness scope — observability, dashboards, alerting, staging load drills, and final release gating — deliberately kept separate from the implementation-hardening work above. T048-T061 remain unimplemented and must not be described as complete. See `plan.md`'s "Feature Status" section for the recorded decision.

**Goal**: Make staging failures diagnosable, alertable, and reversible.

**Independent Test**: Dashboard/alert drills pass before load test.

### Tests and Drills

- [ ] T048 [P] [US5] Add synthetic metric/log emission tests where practical in `tests/Feature/`
- [ ] T049 [US5] Run manual staging alert drill for queue age, DB pressure, breaker open, Redis unavailable, and tenant skew
- [ ] T050 [US5] Run failed-job replay drill using reconciliation command and documented runbook
- [ ] T051 [US5] Run hot-tenant plus 99 normal tenants staging load test and capture results in `docs/`

### Implementation

- [ ] T052 [US5] Emit required operational log context from ingestion services/jobs in `app/Services/`, `app/Jobs/`, and controllers
- [ ] T053 [US5] Add metrics for request rate, latency, rejection reason, queue depth/age, worker drain, DB pressure, breaker state, and tenant skew in `app/Support/` or configured metrics sink
- [ ] T054 [US5] Create dashboards or dashboard definitions for required signals under `docs/` or observability configuration
- [ ] T055 [US5] Create alert definitions or alert documentation for required thresholds under `docs/`
- [ ] T056 [US5] Create operational runbooks for overload, breaker, Horizon scaling, failed-job replay, tenant throttling, Redis degradation, and rollback under `docs/`

**Checkpoint**: Staging load test is observable, actionable, and reversible.

---

## Final Phase: Release Gate and Cleanup

**Status: Blocked on the deferred US5 phase above (T048-T061), not yet started.** This release gate presumes US5's observability/drill work is complete; it is intentionally deferred alongside US5 per `plan.md`'s "Feature Status" section, not silently skipped.

- [ ] T057 Run full focused resilience test suite and document results in `docs/`
- [ ] T058 Run route/provider/license regression tests to protect unrelated behavior
- [ ] T059 Validate no P0/P1 resilience findings remain against `specs/001-100-tenant-resilience/spec.md`
- [ ] T060 Confirm `remove-webapp-forwarding` fallback branch remains untouched and rollback path remains usable
- [ ] T061 Prepare final architecture readiness review before production-like release

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 Setup**: No dependencies.
- **Phase 2 Foundation**: Depends on setup.
- **US1, US2, US3**: Can begin after foundation, but US1 should be MVP priority.
- **US4**: Can begin after queue router foundation and should complete before 100-tenant load confidence.
- **US5**: Starts early for baseline, completes after all operational signals are implemented.
- **Final Release Gate**: Depends on selected stories and staging drills.
- **US1 batch partial-failure safety**: T020, T020a, T020b, and T020c depend on T018a so reconciliation and transaction-boundary work build on the corrected async batch status/dispatch semantics.
- **Horizon processing capacity increases**: T046 may split supervisors earlier, but worker capacity increases are gated on T020b passing transaction-boundary validation.
- **Sharding correctness is UNBLOCKED and should complete before the rest of US3 closes out**: T040-T043 (replacing hardcoded `% 8` routing in `app/Http/Controllers/TestTransactionController.php:104,206`, `app/Http/Controllers/API/V1/RetryHistoryController.php:161,206`, `app/Jobs/BulkGenerateTransactionsJob.php:47`, and `app/Console/Commands/TestTransactionPipeline.php:36`) have no dependency on any remaining US3 task. These paths already disagree with `IngestionQueueRouter`'s canonical `crc32((string)$tenantId) % $shardCount` algorithm (`app/Services/IngestionQueueRouter.php:18-23,40-43`) used by the real Horizon queue family (`config/horizon.php:20-23,85-87,133-134`) — tenant traffic is inconsistently sharded in production code paths today, independent of US3. Complete T040-T043 before finishing T028/T028b/T029-T031/T035/T036/T037, not after; see plan.md's "Revised Implementation Order" for full sequencing and rationale.
- **T028 depended on T028a; T028a is now resolved**: T028a's half-open semantics decision is recorded and approved in `specs/001-100-tenant-resilience/adr/T028a-half-open-circuit-breaker-semantics.md`, so T028's Redis circuit breaker unit tests can now be written/finalized against that decision. T028b (the circuit-breaker `Retry-After` header gap) is independent follow-up work — it does not block and is not blocked by T028.
- **Fairness implementation order (T044, T038, T045, T039, T046)** — approved architecture recorded in plan.md's "Fairness Architecture" subsection:
  1. **T044** (fairness service + config) — no blocking dependency, can start immediately.
  2. **T038** (feature tests against T044's interface).
  3. **T045** (wire fairness middleware into the real `/transactions/official` and `/transactions/batch` routes, after payload-size/backpressure/circuit-breaker).
  4. **T039** and **T046** — T046 must not be marked complete until T039 exists and passes (completion cannot rest on manual inspection of `config/horizon.php` alone); T046's supervisor-count/capacity increase remains separately gated on T020b per the existing dependency noted above. Both the T020b gate and the new T039 gate apply to T046 simultaneously — neither replaces the other.

### MVP Scope

MVP for controlled staging load readiness is US1 + US2 + US3 with enough US5 observability to diagnose the run.

### Parallel Opportunities

- T002, T003, T003a, T003b, T004 can run in parallel.
- T006, T007, T011 can run in parallel.
- Tests within each user story can be written in parallel.
- US2 payload/batch limits can proceed in parallel with US3 circuit breaker work after foundation.
- US4 hardcoded routing replacements (T040-T043) can be split by file and are unblocked now; per the note above, prioritize finishing them before the remaining US3 tasks (T028, T028b, T035, T036) close out. T028a is resolved and no longer part of this remaining-work list.

## Implementation Strategy

1. Preserve current remediation branch and fallback branch safety.
2. Write failing tests for each priority release blocker.
3. Implement the smallest safe change for each story.
4. Run focused tests after each story.
5. Run staging in observe mode before enforce mode.
6. Treat production-like release as blocked until P0/P1 criteria pass.
